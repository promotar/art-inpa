# Connection Method

## Verified 2026-06-28

Laravel server:

```text
Host: 10.10.0.20
User: root
SSH tool: PuTTY plink/pscp from Windows
Laravel path: /var/www/store.z4rank.com/laravel
```

Working command shape:

```text
plink -ssh -batch root@10.10.0.20 "cd /var/www/store.z4rank.com/laravel && php artisan --version"
pscp local-file root@10.10.0.20:/var/www/store.z4rank.com/laravel/codex_tmp/
```

Codex automation can also connect with Paramiko over SSH as `root@10.10.0.20` using the existing local credential source, then operate inside:

```text
/var/www/store.z4rank.com/laravel
```

Credential source:

```text
passwords.txt
```

Do not copy the plaintext password into reports or public documentation.

## Local Docker

- Root: `D:\VPS\art-inpa`
- URL: `http://localhost:8088`
- Vite: `http://localhost:5173`
- Start: `docker compose up -d --build`
- Shell: `docker compose exec app sh`
- Logs: `docker compose logs -f app web queue scheduler vite`
- Database host: `vps-mysql` through external Docker network `vps-internal`
- Database name: `art_inpa_local_dev`
- Credentials: local `passwords.txt` and ignored `.env`/Docker secret files.
